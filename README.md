# Kilden Lands

## Generating the tiles

From the base directory, run the script `GENERATE_TILES`. 

```sh
./GENERATE_TILES.sh
```

The script will look for a file `source.jpg` in the root folder. (Excluded from version control.)